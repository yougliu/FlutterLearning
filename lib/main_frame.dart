import 'package:flutter/material.dart';

//主页面
class MainFrame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text('MainFrame',style: new TextStyle(color: Colors.red),),
    );
  }
}
